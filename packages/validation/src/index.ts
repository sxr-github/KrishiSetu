/**
 * Shared validation schemas for KrishiSetu.
 *
 * Phase 0: package foundation only. Request/response schemas are introduced in
 * later phases alongside the backend endpoints they validate.
 */
export const VALIDATION_PACKAGE_NAME = '@krishisetu/validation';
