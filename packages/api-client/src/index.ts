/**
 * Shared API client for the KrishiSetu web and mobile apps.
 *
 * Phase 0: package foundation only. Typed API methods are introduced in later
 * phases, once the backend endpoints exist.
 */
export const API_CLIENT_PACKAGE_NAME = '@krishisetu/api-client';
